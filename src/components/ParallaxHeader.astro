
  <!-- ✅ Parallax Header -->
  <header class="parallax-header">
    <!-- ✅ Background Video -->
    <video id="bgVideo" class="bg-video" autoplay muted loop playsinline>
      <source src="/bg.mp4" type="video/mp4">
      <!-- Fallback -->
      Your browser does not support the video tag.
    </video>

    <!-- ✅ Overlay -->
    <div class="overlay"></div>

    <!-- ✅ Header Content -->
    <div class="header-content">
      <h1>Same Day Appliance Repair</h1>
      <p>Need fast, reliable appliance repair? Look no further! Our team offers same-day, expert repair services for all major home and commercial appliances...</p>
      <p>We’re trusted by customers throughout Los Angeles to repair top brands like KitchenAid, Frigidaire, LG, Samsung, GE, Sub-Zero, Wolf, and more...</p>
    </div>

    <!-- ✅ Menu Buttons -->
    <div class="menu">
      <a class="button" href="#price-list">Price List</a>
      <a class="button" href="#wolf-stove-repair">Repair</a>
      <a class="button" href="#wolf-stove-installation">Installation</a>
      <a class="button" href="#wolf-stove-cleaning">Cleaning</a>
    </div>
  </header>

  <!-- ✅ Styles -->
  <style>
    /* ✅ Parallax Header */
    .parallax-header {
        position: relative;
        height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        background: url("/bg.webp") no-repeat center center/cover; /* Preload image */
        background-attachment: fixed;
        overflow: hidden;
    }

    /* ✅ Background Video */
    .bg-video {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        opacity: 0; /* Initially hidden */
        transition: opacity 1s ease-in-out;
        z-index: -1;
    }

    /* ✅ Dark Overlay */
    .overlay {
        position: absolute;
        inset: 0;
        background: rgba(0, 0, 0, 0.3);
        z-index: 0;
    }

    /* ✅ Header Content */
    .header-content {
        position: relative;
        text-align: center;
        color: white;
        max-width: 800px;
        padding: 20px;
        background: rgba(0, 0, 0, 0.6);
        border-radius: 10px;
        backdrop-filter: blur(5px);
    }

    /* ✅ Buttons */
    .menu {
        position: absolute;
        bottom: 20px;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        gap: 15px;
    }

    .button {
        text-decoration: none;
        background: rgba(255, 255, 255, 0.2);
        color: white;
        padding: 10px 20px;
        border-radius: 25px;
        font-weight: bold;
        transition: background 0.3s;
    }

    .button:hover {
        background: rgba(255, 255, 255, 0.4);
    }

    /* ✅ Mobile Optimizations */
    @media (max-width: 768px) {
        .bg-video {
            display: none; /* Disable video on mobile */
        }

        .header-content {
            padding: 15px;
            font-size: 14px;
        }

        .menu {
            flex-direction: column;
            bottom: 10px;
        }
    }
  </style>

  <!-- ✅ Script to Fade in Video -->
  <script defer>
    window.addEventListener("load", () => {
        const video = document.getElementById("bgVideo");
        if (video) {
            video.style.opacity = "1"; // Fade in video after loading
        }
    });
  </script>

